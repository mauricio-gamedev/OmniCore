name: Archived PS1 Modern Control Engine v2.1 R5 Build

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R5 workflow archived after device feedback. Use PS1 Adaptive Arc R6 Full APK.'
