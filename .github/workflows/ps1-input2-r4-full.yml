name: Archived PS1 Modern Control Engine v2 R4 Build

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R4 workflow archived after device validation. Use PS1 Modern Control Engine v2.1 R5 Full APK.'
