name: Archived PS1 Input 2.0 R3 Build

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R3 workflow archived. Development continues in PS1 Input 2.0 R4.'
